modules = ["php-8.2", "web", "python-3.11", "nodejs-20"]
[nix]
channel = "stable-25_05"
packages = ["cargo", "libev", "libiconv", "libxcrypt", "openssh", "openssl", "pkg-config", "postgresql", "rsync", "rustc", "unzip"]

[agent]
expertMode = true

[userenv]

[userenv.shared]
ENCRYPTION_KEY = "fSkC3SARMDjYizobXbESgx3cCbFzTG3qJZntkuEpRxE="

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[workflows.workflow.metadata]
outputType = "webview"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = """
cd vps_manager && python -c \"from app import app, db, socketio; 
with app.app_context(): 
    db.create_all()
socketio.run(app, host='0.0.0.0', port=5000, debug=True)\""""
waitForPort = 5000

[[ports]]
localPort = 5000
externalPort = 80

[deployment]
deploymentTarget = "autoscale"
run = ["gunicorn", "--bind", "0.0.0.0:5000", "main:app"]
build = ["sh", "-c", "pip install -r vps_manager/requirements.txt 2>/dev/null || echo 'No requirements file found'"]
